{{- if .Values.vmConnectivity.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: time-to-failure-predictor
  namespace: microshift-001
spec:
  to:
    kind: Service
    name: time-to-failure-predictor-vm
  port:
    targetPort: 30889
  tls:
    termination: edge
{{- end }}
