{{- if .Values.vmConnectivity.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: minio
  namespace: microshift-001
spec:
  to:
    kind: Service
    name: minio-microshift-vm
  port:
    targetPort: 30090
  tls:
    termination: edge
{{- end }}

